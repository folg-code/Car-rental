[project]
name = "my-django-project"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "django>=5.2",
    "psycopg[binary]",
    "django-environ",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-django",
    "ruff",
    "pre-commit",
]